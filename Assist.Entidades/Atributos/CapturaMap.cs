using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Assistente.Entidades.Atributos
{
    public static class CapturaMap
    {
        //public static MapAttribute RetMap(Propriedade prop)
        //{
        //    MapAttribute map = null;
        //    Type t = Type.GetType(prop.Entidade.NameSpace+"." + prop.Entidade.Nome);
        //    if (t != null)
        //    {
        //        MapAttribute[] attribs =(MapAttribute[])t.GetProperty(prop.Nome).GetCustomAttributes(typeof(MapAttribute), true);
        //        if (attribs.Count() > 0)
        //            map = (MapAttribute)attribs[0];
        //    }
        //    return null;
        //}
    }
}
